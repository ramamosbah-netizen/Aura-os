import { NextRequest, NextResponse } from 'next/server'; import { apiBase, apiFetch, authHeader, replayHeaders } from '@/lib/api';
export async function GET(_r:NextRequest,{params}:{params:Promise<{id:string}>}){const {id}=await params;try{const r=await apiFetch(`${apiBase()}/api/v1/contracts/contracts/${id}/amendments`,{method:'GET',headers:await authHeader(),cache:'no-store'});return new NextResponse(r.body,{status:r.status,headers:{'content-type':'application/json'}});}catch{return NextResponse.json({error:'Contracts API unreachable'},{status:502});}}
export async function POST(request:NextRequest,{params}:{params:Promise<{id:string}>}){const {id}=await params;const body=await request.text();try{const r=await apiFetch(`${apiBase()}/api/v1/contracts/contracts/${id}/amendments`,{method:'POST',headers:{'content-type':'application/json',...(await authHeader()),...replayHeaders(request)},body,cache:'no-store'});return new NextResponse(r.body,{status:r.status,headers:{'content-type':'application/json'}});}catch{return NextResponse.json({error:'Contracts API unreachable'},{status:502});}}
