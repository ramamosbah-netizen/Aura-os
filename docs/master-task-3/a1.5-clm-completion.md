# Wave A1.5 — Contracts / CLM final completion run

## Scope and disposition

This run preserved the A1–A1.4 contract, revision, negotiation, client-share,
amendment and Contract 360 work. It did not reopen Sales/Commercial, Project
Delivery, C1–C6 or start Procurement.

Migration `0280_contract_clm_parent_integrity.sql` adds composite tenant +
contract + revision foreign keys. Earlier probes showed that tenant RLS alone
could not prevent a child row pairing a valid revision UUID with a different
tenant/contract. The database now enforces the same frozen revision lineage as
the application services.

## Evidence

- Local PostgreSQL: `aura-os-postgres-1`, database `aura`, migration ledger
  280/280.
- Revisions, negotiation items, client shares and amendments all have RLS
  enabled and forced, with tenant policies. `aura_app` is NOSUPERUSER and
  NOBYPASSRLS.
- Rollback-only app-role checks showed own-tenant writes/reads and zero foreign
  visibility for all four tables. A cross-tenant revision association was
  rejected by migration 0280's composite foreign key.
- Contracts package: 49/49 tests; API Contract controller: 4/4; Web Contract
  fitness: 4/4; Contracts/API/Web typechecks and builds: PASS; migration policy
  280/280; `git diff --check`: PASS.
- The canonical Docker API was rebuilt from current source and started with a
  temporary local NOBYPASSRLS role. `/api/v1/health` reported schema 280/280 and
  projections ready. This role is local-only evidence infrastructure.
- The existing authenticated browser session was tied to the remote Supabase
  profile; its Contract record was absent from local PostgreSQL. A fresh local
  Web tab reached the local image but required authentication, so no local
  authenticated mutation journey is claimed.

## Remaining closure gaps

This continuation also removed the legacy fabricated-zero domain default: `makeContract` now
requires an explicit finite non-negative value (including an intentional `0`) and rejects absent,
NaN, or negative values. Contract closeout now has explicit BFF/API command routes backed by the
existing lifecycle transition authority; Contract 360 presents open obligations, active bonds,
and open amendments as warnings before completion.

The following approved internal CLM capabilities remain partial:

- canonical revision Approval/SoD integration with platform Approvals (the
  current implementation has the revision-author self-approval guard);
- contract-specific DMS association/upload and complete business-history
  aggregation;
- renewal/extension: `ContractStatus` has no expiry/renewal state or command; project time
  extensions remain owned by Project EOT/C6. This is therefore `NOT APPLICABLE — PROVEN CURRENT
  DOMAIN BOUNDARY`, not an unimplemented Contract writer;
- complete capability-permission enforcement and authorized/unauthorized proof;
- concurrency/replay proof against PostgreSQL for revision, share and amendment
  writers; and
- authenticated browser proof against the local API + PostgreSQL runtime.

Client-share records intentionally stop at `prepared`/`dispatched` unless an
external provider confirms delivery. No customer email was sent.

## Final status

`MASTER TASK 3 — WAVE A1 CONTRACTS = PARTIAL` — the remaining items are
product/evidence gaps, not reasons to weaken protected authorities.
