# §22 Step 2A — schedule task identity proof

_Generated 2026-09-09T08:45:42.769Z against the disposable database._

## Migration chain
Applied from zero: **284** · head `0284_schedule_task_identity.sql`

## 1. Structure and RLS
  ✓ aura_projects_schedule_tasks exists
  ✓ relrowsecurity
  ✓ relforcerowsecurity
  ✓ has a policy — 1
  ✓ composite lineage UNIQUE (tenant_id, project_id, schedule_id, id) exists for Step 2B to key on
  ✓ no duration column — that is Step 2B, and inferring it here would fabricate authored input

## 2. Backfill of a synthetic legacy schedule

An empty database proves nothing about a backfill, so a pre-2A schedule is built by hand —
a JSONB `tasks` array with **duplicate names** and a task carrying a baseline — and the
migration's own backfill statement is re-run against it.

  ✓ N JSONB tasks → N rows — 3 → 3
  ✓ duplicate names survive as two rows — not deduplicated
  ✓ and they receive DISTINCT identities
  ✓ baseline copied exactly, not recomputed — 2026-03-01
  ✓ including a baseline that DIFFERS from planned — a slipped task keeps its commitment — 2026-03-04
  ✓ a task with no baseline keeps none — no fabricated value
  ✓ progress preserved — 40

## 3. The legacy column is still there
  ✓ aura_projects_schedules.tasks is intact — rollback has somewhere to land — 3 task(s)

## 4. Isolation, from aura_app (NOSUPERUSER, NOBYPASSRLS, non-owner)
  ✓ tenant B cannot READ tenant A's schedule tasks
  ✓ tenant B cannot UPDATE them — 0 rows
  ✓ tenant B cannot DELETE them — 0 rows
  ✓ tenant B cannot INSERT a row carrying tenant A's tenant_id
  ✓ tenant A CAN read its own — the policy is not simply denying everything — 3 row(s)

## 5. Constraints
  ✓ percent_complete outside 0..100 is rejected
  ✓ planned_end before planned_start is rejected
  ✓ a task pointing at a schedule that does not exist is rejected

## 6. Runtime — schedule API over HTTP

  ✓ authenticated as u-admin
  ✓ created a project — HTTP 201
  ✓ saved a schedule with two identically named tasks — HTTP 201
  ✓ the API returns two tasks with DISTINCT ids
  ✓ baseline captured for both
  ✓ both ids survived the save
  ✓ the rename applied
  ✓ the renamed task KEPT ITS OWN baseline — 2026-03-01..2026-03-05
  ✓ the other same-named task kept its own, unstolen
  ✓ a task row exists in aura_projects_schedule_tasks for that id
  ✓ the API reads TASK ROWS as authority, not the JSONB mirror

**All checks passed.** Every seeded row was deleted.
