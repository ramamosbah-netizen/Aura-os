# §22 Step 2B — duration and dependency proof

_Generated 2026-09-09T09:31:43.915Z against the disposable database._

## Migration chain
Applied from zero: **285** · head `0285_schedule_duration_dependencies.sql`

## 1. Authored duration
  ✓ a task created without a duration keeps NULL — nothing was inferred from its dates
  ✓ a zero duration is rejected
  ✓ a negative duration is rejected
  ✓ an authored duration persists — 4 working days

## 2. Dependency lineage
  ✓ an edge between two tasks in the SAME project and schedule is accepted
  ✓ a self-dependency is rejected BY THE CHECK CONSTRAINT — new row for relation "aura_projects_schedule_dependencies" violates check constraint "aura_projects_schedule_dependencies_self_check"
  ✓ a duplicate edge is rejected by UNIQUE — duplicate key value violates unique constraint "uq_aura_projects_schedule_dependencies_edge"
  ✓ an edge reaching a task in ANOTHER PROJECT is rejected by the composite FK — insert or update on table "aura_projects_schedule_dependencies" violates foreign key constraint "aura_projects_schedule_dependencies_successor_fkey"
  ✓ an edge whose predecessor belongs to a different schedule is rejected — insert or update on table "aura_projects_schedule_dependencies" violates foreign key constraint "aura_projects_schedule_dependencies_predecessor_fkey"
  ✓ an edge claiming another tenant is rejected BY THE COMPOSITE FK, not by UNIQUE — insert or update on table "aura_projects_schedule_dependencies" violates foreign key constraint "aura_projects_schedule_dependencies_predecessor_fkey"

## 3. Cycles are a graph invariant, owned by the domain
  ✓ the DATABASE accepts a two-hop cycle — judging it needs the whole edge set, not one row — this is by design; the governed writer rejects it, and 18 domain tests pin that

## 4. Deleting a task removes its edges
  ✓ edges cascade with the task, so no edge outlives an endpoint — 2 → 0

## 5. RLS on the dependency table
  ✓ relrowsecurity
  ✓ relforcerowsecurity
  ✓ has a policy — 1
  ✓ another tenant cannot read the dependency network
  ✓ the owning tenant can — 1 edge(s)

**All checks passed.** Every seeded row was deleted.
