# §22 Step 4 — resource pool and capacity proof

_Generated 2026-09-09T09:51:26.120Z against the disposable database._

## Migration chain
Applied from zero: **286** · head `0286_resource_pools_capacity.sql`

## 1. Authority — organisation-scoped, not project-owned
  ✓ neither table carries a project_id — Design Gate §3.1, and the reason cross-project detection can work at all — none

## 2. Pool constraints
  ✓ an internal pool is created
  ✓ a second pool of the same name in the same scope is rejected — two would be counted independently — duplicate key value violates unique constraint "uq_aura_projects_resource_pools_name"
  ✓ an unknown unit is rejected
  ✓ a subcontracted crew with no supplier behind it is rejected — new row for relation "aura_projects_resource_pools" violates check constraint "aura_projects_resource_pools_subcontract_check"
  ✓ one supplier may field several crews, each with its own identity — supplierId is never poolId
  ✓ the same name in a different org scope is a different pool

## 3. Capacity
  ✓ an UNKNOWN capacity is storable as NULL — not 0, and not a sentinel for unlimited
  ✓ a declared ZERO is a different, storable fact
  ✓ a negative capacity is rejected
  ✓ a backwards interval is rejected
  ✓ an unknown resource type is rejected — a reference is typed, never a free string
  ✓ overlapping windows are ALLOWED — two hire periods genuinely give two cranes, and the domain sums them

## 4. Isolation, from aura_app (NOSUPERUSER, NOBYPASSRLS, non-owner)
  ✓ aura_projects_resource_capacity: ENABLE + FORCE + policy — policies=1
  ✓ aura_projects_resource_pools: ENABLE + FORCE + policy — policies=1
  ✓ tenant B cannot read tenant A's pools
  ✓ tenant B cannot read tenant A's capacity
  ✓ tenant B cannot update them — 0 rows
  ✓ tenant B cannot INSERT a pool carrying tenant A's tenant_id
  ✓ tenant A can read its own pools — the policy is not simply denying everything — 4 pool(s)

**All checks passed.** Every seeded row was deleted.
