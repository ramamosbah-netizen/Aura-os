# §22 Step 6 — resource booking proof

_Generated 2026-09-09T10:37:31.942Z against the disposable database._

## Migration chain
Applied from zero: **288** · head `0288_resource_bookings.sql`

## 1. Current feasibility is NOT a column
  ✓ no column stores a feasibility verdict — it is derived on every read, never remembered — 20 columns, none of them a verdict
  ✓ the COMMITMENT SNAPSHOT is stored — what was known when the promise was made

## 2. The temporal invariant, demonstrated
  ✓ the booking fits when it is made — AVAILABLE
  ✓ the SAME booking is conflicted once capacity drops, with no write to the booking — CONFLICTED
  ✓ and the commitment snapshot is untouched — history was not rewritten — capacity_at_commitment 2, demand 1, status held
  ✓ so the record still says BOTH: it fitted when committed, and it does not fit now

## 3. Committing over capacity is governed, not blocked
  ✓ a SILENT overrun is rejected — committing past a known capacity costs a sentence — new row for relation "aura_projects_resource_bookings" violates check constraint "aura_projects_resource_bookings_over_capacity_check"
  ✓ and whitespace is not a reason — new row for relation "aura_projects_resource_bookings" violates check constraint "aura_projects_resource_bookings_over_capacity_check"
  ✓ WITH a reason it is accepted — blocking would not prevent the overrun, only the record of it
  ✓ no reason is demanded when capacity is UNKNOWN — nobody can justify exceeding an unrecorded number

## 4. What a booking refuses to be
  ✓ zero held is rejected — that is the absence of a booking, not a booking — new row for relation "aura_projects_resource_bookings" violates check constraint "aura_projects_resource_bookings_quantity_check"
  ✓ a snapshot whose total demand is less than this booking alone is rejected as incoherent — new row for relation "aura_projects_resource_bookings" violates check constraint "aura_projects_resource_bookings_demand_check"
  ✓ a range that ends before it starts is rejected
  ✓ an unknown resource type is rejected — a reference is typed, never free text
  ✓ an unknown unit is rejected
  ✓ 'conflicted' is not a status a booking can be IN — it is a verdict ABOUT one, and is not stored — new row for relation "aura_projects_resource_bookings" violates check constraint "aura_projects_resource_bookings_release_check"

## 5. Releasing
  ✓ a release with no reason is rejected — it is indistinguishable from a mistake — new row for relation "aura_projects_resource_bookings" violates check constraint "aura_projects_resource_bookings_release_check"
  ✓ a HELD booking carrying release fields is rejected — new row for relation "aura_projects_resource_bookings" violates check constraint "aura_projects_resource_bookings_release_check"
  ✓ a release with a reason and a time is accepted
  ✓ the released booking is still on the record — capacity is given back, history is not deleted — 4 bookings, 3 held
  ✓ the engine's index is PARTIAL on held — a released booking cannot reach a conflict total — (status = 'held'::text)

## 6. Lineage — a booking cannot claim another tenant's project
  ✓ a booking naming this tenant's project under ANOTHER tenant is rejected BY THE COMPOSITE FK — insert or update on table "aura_projects_resource_bookings" violates foreign key constraint "aura_projects_resource_bookings_project_fkey"
  ✓ a booking naming a project that does not exist is rejected — insert or update on table "aura_projects_resource_bookings" violates foreign key constraint "aura_projects_resource_bookings_project_fkey"
  ✓ a task reference is an ADDRESS, not an integrity claim: it is accepted and may stop resolving — AURA-PM-004 — schedule saves delete and re-insert task rows, so no FK is safe here

## 7. Isolation, from aura_app (NOSUPERUSER, NOBYPASSRLS, non-owner)
  ✓ relrowsecurity
  ✓ relforcerowsecurity
  ✓ has a policy — 1
  ✓ the actor is NOSUPERUSER and NOBYPASSRLS, so this proof is not vacuous
  ✓ another tenant cannot read this tenant's commitments
  ✓ the owning tenant can — the policy is not simply denying everything — 5 booking(s)

**All checks passed.** Every seeded row was deleted.
