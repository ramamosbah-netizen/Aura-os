# P1 Tier Closure — Gaps #6–#15 (2026-07-08)

All ten **P1 enterprise-credibility** rows of the Vol 23 gap register closed in one pass,
each verified against the live tree. Companion docs: `2026-07-08-performance-baseline.md`
(the #15 deliverable), `infrastructure/observability/README.md` (the #6 runbook).

## What shipped, per gap

| # | Close-out | Key files |
|--:|---|---|
| 6 | OTLP/HTTP metrics push (config-gated, dependency-free) + `http_requests_total`/duration middleware + Prometheus alert pack | `core/src/observability/otlp.ts` (+5 tests) · `apps/api/src/main.ts` · `infrastructure/observability/prometheus-alerts.yml` + README |
| 7 | Route-derived default permissions — guard derives `module.entity.action` from declared routes when no `@Permissions` decorator: **all handlers covered by construction**; explicit decorators override; health/auth/metrics exempt | `core/src/identity/permissions.guard.ts` (`derivePermissionFromRoute`, 9 tests) |
| 8 | Quotation + subcontract form schemas relocated to shared and enforced server-side (`assertFormValid`) on `POST /crm/quotations` and `POST /subcontracts`; every metadata-form endpoint now enforces (engineering-documents has no endpoint by design) | `shared/src/forms/schemas/{quotation,subcontract}.ts` · both controllers |
| 9 | Site child lists (`delay-logs`, `material-consumption`, `instructions`, `labour`) got `/paged` routes; **frontend opt-in exemplar**: `/crm/accounts` fetches `/paged` (50/window) with a server-rendered pager | `modules/site/src/site.service.ts` · `apps/api/src/site/site.controller.ts` · `apps/web/app/crm/accounts/page.tsx` |
| 10 | AR aging CSV (`customer-invoices/aging.csv`, pure `arAgingCsvRows` + test) · supplier-invoice register CSV (`invoices/export.csv`) · download buttons on AR/AP aging + invoices + filter-aware audit export via BFF csv proxies | `modules/finance/src/domain/ar-aging.ts` · finance controller · 4 BFF routes · 4 pages |
| 11 | Per-user recipient resolution: `NOTIFY_RECIPIENTS` map → address-shaped userId passthrough → tenant fallback | `core/src/notifications/notification.service.ts` (`resolveRecipient`, +2 tests) |
| 12 | PG-backed roles/grants: write-through + hydrate-on-boot (authz decisions stay sync in-memory) | migration `0133_access_roles_grants` · `core/src/identity/access.service.ts` (+2 tests) |
| 13 | Persisted TOTP MFA (two-step enroll→activate) gating `POST /auth/login`; bad codes share the brute-force lockout; `AUTH_GROUP_ROLE_MAP` maps IdP `groups` claims to AURA role grants idempotently on token verify | migration `0134_user_mfa` · `core/src/identity/mfa.service.ts` (+6 tests) · auth controller/service |
| 14 | AES-256-GCM field crypto (`enc:v1:` format, random IV, auth-tag fail-closed, legacy passthrough, staged by `PII_ENCRYPTION_KEY`); wired for WPS identifiers `iban` + `molEmployeeId` at the HR Postgres store boundary | `shared/src/security/field-crypto.ts` (+8 tests) · `modules/hr/src/postgres-employee-store.ts` |
| 15 | Perf baseline harness (`pnpm --filter @aura/api perf`, budgets in-script, `--enforce` for CI) + measured baseline + budgets doc | `apps/api/scripts/perf-baseline.mjs` · `docs/reports/2026-07-08-performance-baseline.md` |

## Verification (all on the live tree, fresh dist)

- **Pipeline**: turbo build **22/22** · typecheck **42/42** · test tasks **41/41** (core 125→131*, shared 151, finance 110, hr 58 among them; ~40 new tests this pass).
- **#9**: all 4 site `/paged` routes → 200 with `Page<T>` envelope; `/crm/accounts` renders through the paged fetch.
- **#10**: `ar-aging.csv` / `ap-aging.csv` / `invoices/export.csv` → 200 with correct headers; BFF proxies → 200; download buttons render.
- **#12**: role+grant created via `/admin/access` → **API restarted** → "Hydrated 3 role(s) + 3 grant(s) from Postgres", rows still listed. Test artifacts cleaned up.
- **#13**: enroll → wrong code rejected (401) → valid TOTP code → `{"active":true}`.
- **#8**: invalid quotation submit → 400 `VALIDATION` via the taxonomy.
- **#6**: `/metrics` correctly 404s while `METRICS_ENABLED` unset (gating verified); OTLP pusher covered by unit tests (no collector in dev).
- **#15**: baseline run twice (n=40); found one stale-process measurement trap (old dist on :4000) — re-measured on fresh dist; results + the `GET /events` hotspot documented.

\* core suite count grew 125→131 during the pass as guard/MFA/OTLP tests landed.

## Notes & follow-ons (not gaps — hygiene)

1. `GET /events` unwindowed feed is the one real perf hotspot (~675ms normalized p95) — cap/paginate the default feed.
2. `pagePostgres` COUNT+SELECT could run `Promise.all` — halves paged latency on high-RTT links.
3. PII crypto pattern is set on HR WPS identifiers; extend field-by-field as the PII catalog grows.
4. Enforcement reminder: permission taxonomy (#7) and MFA gate (#13) engage when auth is ON (`AUTH_JWT_SECRET`/`AUTH_JWKS_URL`) — staged pass-through preserved for dev.

**Register state after this pass: P0 #1/#3/#4/#5 are the only open sell/deploy blockers.**
