# P2 Wave 1 — Published SDK, Data Lifecycle, Weak-Module Test Depth

**Date:** 2026-07-09 · **Branch:** `feat/p2-depth-wave1`
**Closes:** gap register (Vol 23) **P2 #21, #25, #27** — the S–M rows of the tier, opened the
same day P0 finished. The L rows (#16 remainder, #17–#20, #22–#24, #26) stay honestly open.

---

## 1. #21 — Published SDK (`@aura/sdk`)

- New workspace package `packages/sdk`: hand-written core (`client.ts`) + generated surface
  (`generated.ts`, **646 operations** from the live OpenAPI doc at `/api/docs-json`).
- The core carries the platform contracts: **`AuraApiError`** maps the enforced error
  taxonomy (400→VALIDATION, 401→AUTH, 403→FORBIDDEN, 404→NOT_FOUND, 409→CONFLICT,
  429→RATE_LIMITED, 5xx→SERVER), `Idempotency-Key` per call, `Page<T>`, `setToken` rotation.
- Generator: `scripts/generate-sdk.mjs` — uses `SPEC_URL` or boots the built API in-memory
  (no DB) on a scratch port. **CI drift gate**: the `deploy-readiness` job regenerates
  against the live booted API and `git diff --exit-code`s the generated file.
- Verified live: boot → `authLogin` → `crmAccountsCreate` (with idempotency key) →
  `crmAccountsPaged` → missing id rejects as `AuraApiError NOT_FOUND`. 5 unit tests.
- Honest limits: payload types are `unknown` until DTOs gain swagger schemas; the old
  `SdkGeneratorService` command-stub emitter in core is superseded.

## 2. #25 — Data lifecycle (migrations / orphans / archiving)

- **Migration policy decided**: forward-only (recovery = PITR/dump per backup-dr runbook);
  `-- @DOWN` **required from migration 0137 on** as the bad-deploy hatch. CI gate
  `scripts/migration-policy-check.mjs`: `NNNN_snake_case.sql` naming, duplicate numbers,
  **numbering gaps**, @DOWN presence. Verified on the current 136-file chain.
- **Orphan scan** (`pnpm --filter @aura/api db:orphan-scan`): 11 catalogued spine
  cross-context references (tenders→accounts … GRNs→POs), tenant-scoped `not exists`
  checks, `--enforce` for CI. Runs in `deploy-readiness` against the seeded deal chain.
- **Archiver** (`pnpm --filter @aura/api db:archive`): rolls `aura_events` (**processed
  rows only** — pending outbox never moves) + `aura_audit_log` older than N months (default
  12) into `*_archive` twins, transactional batches; **dry-run by default**; CI smoke.
- Policy + cadences + run log: `docs/runbooks/data-lifecycle.md`.

## 3. #27 — Weak-module test depth

All five modules Vol 21 §1 flagged (Engineering ×1, HSE/Site/Assets/DocControl ×2) now hold
**≥4 meaningful test files**, service-level with a recording EventStore and tenant-isolation
assertions:

| Module | New file | What it proves |
|---|---|---|
| Engineering (4) | `design-change.test.ts` | ADR-0011/0012 composition seam: approved+cost-impact emits `…approved` with `triggersVariation: true` + value snapshot (the reactor's Variation trigger); rejection → false; decider stamping; **real AccessService grant** (`engineering.*`) |
| HSE (4) | `permit-incident-capa.test.ts` | PTW request→approve (+`hse.ptw.issued`), incident report→close, CAPA raise→complete, tenant isolation |
| Site (5) | `site-diary-delays.test.ts` | daily-report submit transition (+event with manpower), delay-log/EOT lifecycle (+impact hours), paged windowing, material consumption event |
| Assets (4) | `maintenance-inspection-disposal.test.ts` | maintenance schedule→complete (actual cost overwrites estimate), inspections, **double-disposal state guard**, gain/loss math, name snapshotting |
| DocControl (5) | `correspondence-transmittal-guard.test.ts` | correspondence log→close, transmittal-item **project-mismatch guard**, revision snapshot default, acknowledge transition |

## 3a. Admin surface (same wave, Vol 15 §2.9 tail / §2.6 retention seam)

The #25 tooling is admin-visible, not script-only: `/admin/data` grew a **Data lifecycle**
section — orphan-scan report table (per-reference counts, red pill on hits), retention-window
editor persisting `lifecycle.archiveMonths`, and archiver **dry-run / execute** buttons with
eligibility + already-archived counts. Backed by guarded `GET admin/platform/data-lifecycle`
and `POST admin/platform/archive-run` (execute is audited); the reference catalog moved to
`infrastructure/orphan-references.json` as the single source for the script, the endpoint,
and CI (the api Docker image now ships the whole `infrastructure/` dir).

**First live run against the dev database found 5 real orphans** — 2 tenders, 2
opportunities, 1 contract referencing CRM accounts deleted during development. Left in place
per the runbook (investigate, don't auto-fix); visible on the admin page.

## 4. Verification

- Module tests: Engineering 36 · HSE 18 · Site 26 · Assets 20 · DocControl 26 — all green.
- SDK: 5 unit tests + the live smoke above; package builds with declarations.
- Full-repo lint (0 errors) / typecheck / test suite green; CI on the PR exercises the three
  new gates (migration policy, orphan scan, SDK drift) end-to-end — **all 5 jobs green**
  (run 29069939303; the orphan scan's first CI run caught a uuid-vs-text id comparison,
  fixed with a text cast).
- Admin surface: booted the built API against the live dev DB — status endpoint returned the
  orphan report (5 real finds) + archive eligibility; dry-run moved nothing, as designed.
