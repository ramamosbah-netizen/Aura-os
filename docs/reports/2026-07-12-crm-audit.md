# CRM Final Audit — logic, dead code, duplication

**Date:** 2026-07-12 · **Scope:** the CRM surface after the commercial-model wave
(#72–#76) + Relationship Intelligence + the ambient Advisor.

---

## A. Duplication (the important findings)

### A1. Three overlapping "attention" engines
Three endpoints independently recompute *last-activity-per-record* and flag
stalled / inactive / at-risk from the same data:

| Endpoint | UI | Signals |
|----------|----|---------|
| `GET /crm/activities/command` | Activities "Needs attention" panel | inactive accounts, stalled opps |
| `GET /crm/opportunities/pipeline` (atRisk) | Pipeline "Command" view | at-risk deals + recommendations |
| `GET /crm/intelligence/alerts` | **Advisor + Intelligence page** | no-next-action, stalled, inactive-account, no-decision-maker, expiring-quote — **superset** |

The Advisor now shows *stalled-opportunity* and *inactive-account*, which the
Activities "Needs attention" panel **also** shows, and *no-decision-maker* /
close-passed, which the Pipeline at-risk table **also** shows. → **Recommendation:**
make `relationship-intelligence` the single source of truth; have the Activities
panel and the Pipeline at-risk table consume/scope it rather than re-deriving.

### A2. Pipeline "Forecast" tab ⊂ "Command" view
The old **Forecast** tab renders "weighted forecast by expected close month"; the
new **Command** view renders the same table **plus** the weighted-forecast KPI. The
Forecast tab is now redundant. → **Recommendation:** drop the Forecast tab (or fold
it into Command).

---

## B. Dead / non-functional code

### B1. Unified Timeline is unwired
`components/timeline.tsx` + `GET /api/crm/timeline` + `crm-timeline.controller.ts`
form a complete "record timeline" feature that **no page imports or renders**. It's
dead. → **Recommendation:** either wire `<Timeline>` into the 360 pages (Account /
Contact / Opportunity already synthesize their own timelines inline, so this is
duplicative too) or remove the three files + BFF route.

---

## C. Logic / consistency

### C1. Inconsistent "stalled / quiet" thresholds
The same deal can be "stalled" in one view and healthy in another:

| Engine | Account idle | Opp idle |
|--------|--------------|----------|
| activities/command | ≥ 30d | ≥ 14d |
| intelligence/alerts | ≥ 60d | ≥ 14d |
| pipeline (stalled) | — | age ≥14d **and** quiet ≥21d |

→ **Recommendation:** one shared threshold constant (e.g. in `@aura/shared`) used by
all three.

### C2. Three unreconciled "likelihood" signals on an opportunity
`winProbability` (manual %), the AI forecast (`/forecast`), and the BANT
qualification score (0–4) all express "how likely / how real" without a single
derived number. Not a bug — but they can disagree on the same deal. Consider
deriving `winProbability` from stage + BANT unless manually overridden.

### C3. Advisor ↔ AI Copilot right-edge collision — FIXED this pass
Both were anchored bottom/top-right. The Advisor now has a bounded height so it
never covers the Copilot launcher, and the **AI Copilot opens as a right side
window** (matching the Advisor geometry) instead of a small floating box.

---

## D. Minor

- **D1.** Seeded demo data has mojibake ("Marina Retrofit � ELV") from earlier
  em-dash curl seeding — data only, not a code defect. Clean the two rows.
- **D2.** The `nextAction` (text) + `nextActionDueDate` (date) + owner triad now
  drives the Next-Action Invariant consistently — good.

---

## What was changed in this pass — ALL FOUR RESOLVED
- **AI Copilot → right side window** (`ai-dock.tsx`): full-height right-docked panel
  instead of a 380×520 floating box, matching the Advisor.
- **Advisor bounded height** (`crm-advisor.tsx`): anchored top-right with
  `maxHeight` so it no longer overlaps the Copilot launcher.
- **#1 + #4 — one shared attention definition** (`modules/crm/src/attention.ts`):
  `ATTENTION_THRESHOLDS` (account 30d · opp 14d · quote 7d) + `lastActivityByRecord`
  + `daysSince` + `isQuiet`. The activity command, pipeline command and
  relationship-intelligence controllers now all import these instead of each
  re-implementing the last-touch scan and their own thresholds. Verified live: the
  **same 2 stalled opportunities** now surface identically across all three
  endpoints (previously the pipeline used a 21-day cutoff and intelligence a 60-day
  account cutoff — they could disagree).
- **#2 — Pipeline Forecast tab:** already resolved in the working tree — the old
  standalone "Forecast" tab is now folded into the **Analytics** tab (aging + owner
  performance + weighted forecast by month + stalled); Command keeps only the
  weighted-forecast KPI (summary, not a duplicate).
- **#3 — Timeline:** NOT dead after all — it's wired into **Opportunity 360**
  (`<Timeline recordId={o.id} />`), so it stays. (Corrects the earlier finding.)
